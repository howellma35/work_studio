# 2026-07-04 HTTPS 部署全链路调试日志

## 背景

项目采用内网穿透架构部署：家里 Docker 服务 → frp 隧道 → 阿里云 ECS → 公网访问。之前 HTTP 开发端口(6000/6001/6002)已通过 frp 穿透正常工作，但生产域名 `mahongwei.com.cn` 的 HTTPS (443) 访问一直不通。

## 今天解决的问题

### 问题 1：frpc token 与 frps 不一致

**症状**：
- `curl http://mahongwei.com.cn` 返回 Empty reply from server
- `curl https://mahongwei.com.cn` 返回 SSL connect error
- ECS 上 `sudo ss -tlnp | grep frps` 只看到 6000/6001/6002/7000/7500，没有 80/443

**根因**：frpc 日志明确报 `login to the server failed: token in login doesn't match token from configuration`，frpc 从未成功连接 frps，穿透规则从未注册。

**解决**：对比 `/etc/frp/frps.toml` 和 `/etc/frp/frpc.toml` 的 `auth.token`，改为一致后重启 frpc。

**经验**：修改 frpc 配置后只需重启 frpc，frps 无需重启（frps 自动处理新连接注册）。

---

### 问题 2：SSL 证书不存在导致 Docker nginx 反复重启

**症状**：
- `docker ps | grep nginx` 显示 `Restarting (1) 11 seconds ago`
- `docker logs nginx` 报 `cannot load certificate "/etc/letsencrypt/live/mahongwei.com.cn/fullchain.pem": No such file or directory`

**根因**：家里机器从未申请过 SSL 证书，但 nginx.conf 同时配了 HTTP 和 HTTPS（引用证书文件），找不到文件导致 nginx 启动失败。

**决策**：将 SSL 终结从家里 nginx 移到 ECS nginx，这样：
- 证书在 ECS 上申请，certbot standalone 直接验证，不走 frp 隧道
- 家里 nginx 只做 HTTP，不需要证书
- 架构更清晰

**新架构**：
```
用户 → ECS nginx (443 SSL终结) → 127.0.0.1:6003 → frps → frp隧道 → 家里 nginx:80
```

---

### 问题 3：nginx.conf 文件内容重复

**症状**：写入新配置时旧内容没有被覆盖，导致文件中出现两套 server block（旧的 HTTPS + 新的 HTTP-only）。

**解决**：用完整正确的 HTTP-only 配置重写整个文件。

---

### 问题 4：ECS nginx 与 frps 的 80 端口冲突

**症状**：
- ECS nginx 需要监听 80（HTTP→HTTPS 重定向）
- frps 的 nginx-http 穿透规则 `remotePort=80` 也在 ECS 上监听 80
- 两者冲突，nginx 无法启动

**解决**：
- frpc 穿透规则的 `remotePort` 从 80 改为 6003
- ECS nginx 的 `proxy_pass` 改为 `http://127.0.0.1:6003`
- 删除 frpc 中的 `nginx-https`（443）穿透规则（不再需要）

**额外发现**：ECS nginx proxy_pass 到 127.0.0.1:6003 是本机内部通信，UFW 和阿里云安全组**不需要放行 6003 端口**（之前文档里写了放行 6003 是多余的，已删除）。

---

### 问题 5：docker-compose.yml 端口映射未更新

**症状**：`docker compose up -d nginx` 后容器仍映射 443 端口。

**分析**：
- docker-compose.yml 已改为只映射 80，但家里的文件可能没同步
- 即使文件同步了，`docker compose up -d` 默认不重建已有容器（端口映射变更需要 `--force-recreate`）
- 实际上 443 映射不影响功能（nginx 内部不监听 443 就不会有流量进来）

**解决**：同步最新文件后 `docker compose up -d --force-recreate nginx`。

---

### 问题 6：ECS nginx 443 端口 Connection refused

**症状**：证书存在、配置语法通过（`nginx -t` OK），但 `sudo ss -tlnp | grep nginx` 只有 80，没有 443。

**根因**：nginx 启动时 frps 还占着 80 端口（旧穿透规则 remotePort=80），nginx 启动过程中 80 端口绑定失败，导致整个进程异常，HTTPS server block 没有生效。

**解决**：
1. 先修改 frpc 的 remotePort=6003，重启 frpc 释放 80 端口
2. 再 `sudo systemctl restart nginx`，443 端口正常监听

---

### 问题 7：UFW/安全组配置清理

发现文档中有多处多余的安全组规则：
- 6003 端口不需要放行（本机内部通信）
- 443 穿透规则已删除，不需要放行 443 给 frps

已清理文档，保留正确规则：80/443（ECS nginx）、7000（frps）、6000/6001/6002（开发调试）、7500（Dashboard）。

---

### 问题 8：Docker 镜像拉取失败（国内镜像源问题）

**症状**：
- `docker compose up -d --build` 报 `failed to resolve source metadata for docker.io/library/python:3.11-slim: not found`
- 所有需要构建的服务（ai-server、vehicle-backend 等）全部失败
- 直接拉取也超时

**排查过程**：
1. 先尝试国内公共镜像源（docker.1ms.run、docker.xuanyuan.me、docker.rainbond.cc），部分源 DNS 被劫持到 Facebook IP（31.13.76.65）导致超时
2. 尝试配置 Docker 代理（通过 Windows v2rayN 局域网共享），代理能拉取镜像但走外网反而访问国内镜像源变慢
3. 阿里云镜像加速器不支持内网服务器（只支持 ECS）

**解决**：
- 最终用 `docker.1ms.run` + `docker.xuanyuan.me` 两个源成功拉取
- 删除了代理配置（避免干扰国内源），恢复纯国内镜像源方案
- 已整理成独立文档 `docs/docker-镜像源与代理切换.md`，记录三种方案的切换方法

**经验**：
- 代理和国内镜像源不能同时使用（代理走外网，国内源反而不通）
- daemon.json 和 systemd proxy.conf 是两套独立配置，改了一个别忘了另一个
- Docker pull 和 Docker build 可能使用不同的镜像源配置

---

### 问题 9：Docker 代理配置残留导致构建失败

**症状**：删掉代理后 `docker compose up -d --build` 仍然报 `not found`。

**根因**：`/etc/systemd/system/docker.service.d/proxy.conf` 没有被删除干净，Docker 仍在走已失效的代理。

**解决**：
```bash
sudo rm -f /etc/systemd/system/docker.service.d/proxy.conf
sudo rm -rf /etc/systemd/system/docker.service.d
sudo systemctl daemon-reload
sudo systemctl restart docker
```

---

### 问题 10：Docker Compose profiles 导致 Langfuse 未启动

**症状**：`docker compose up -d --build` 成功后，Langfuse 相关容器没有启动。

**根因**：docker-compose.yml 中 Langfuse 服务配置了 `profiles: ["langfuse"]`，默认不启动。

**解决**：去掉所有 `profiles: ["langfuse"]` 配置（共 6 处），Langfuse 随默认启动。

---

### 问题 11：Docker 容器重建后 Nginx 502 Bad Gateway

**症状**：
- `docker compose up -d --build` 全部成功，所有容器正常运行
- 但 `curl http://localhost` 和 `curl https://mahongwei.com.cn` 都返回 502 Bad Gateway
- nginx 错误日志：`connect() failed (111: Connection refused) while connecting to upstream`
- 上游地址 `http://172.18.0.9:80/` 连接被拒

**根因**：`docker compose up -d --build` 重建了 web 容器，容器的 Docker 内部 IP 发生了变化（旧 IP 172.18.0.9 → 新 IP），但 nginx 容器在启动时解析了 DNS 并缓存了旧 IP，没有随容器重建而更新。

**解决**：
```bash
docker compose restart nginx
# nginx 重启后重新解析 DNS，获取到 web 容器的新 IP
curl http://localhost  # 正常返回
```

**经验**：Docker Compose 中 nginx 通过 `upstream` 引用的容器名（如 `web`、`ai-server`），nginx 启动时会解析 DNS 并缓存。如果上游容器被重建（IP 变化），必须 `docker compose restart nginx` 刷新 DNS 缓存。

---

### 问题 12：React Router Link 导航到独立 SPA 白屏

**症状**：
- 首页点击“AutoMind 智能车机助手”卡片后白屏，必须手动刷新页面才显示
- 浏览器前进/后退按钮也会出现白屏

**根因**：主站首页用了 `<Link to="/vehicle/">`（React Router 客户端路由），但 `/vehicle/` 是由**另一个独立容器** vehicle-frontend 提供的全新 SPA，不是主站 React Router 的路由。React Router 拦截导航后在主应用路由表里找不到 `/vehicle/`，渲染白屏。手动刷新时浏览器直接请求 nginx，nginx 正确转发到 vehicle-frontend 容器才正常。

**解决**：把 `<Link to="/vehicle/">` 改成普通 `<a href="/vehicle/">`，触发全页面导航而不是客户端路由。

**经验**：当项目中存在多个独立的 SPA（由 nginx 不同 location 路由到不同容器），跨应用导航必须用 `<a>` 标签，不能用 React Router 的 `<Link>`。

---

### 问题 13：nginx upstream DNS 缓存导致每次重建都要重启

**症状**：
- 每次 `docker compose up -d --build` 重建容器后，nginx 报 502 Connection refused
- 必须 `docker compose restart nginx` 才能恢复
- 业界主流不应该每次都要重启 nginx

**根因**：nginx 默认的 `upstream` 块在启动时解析 DNS 并永久缓存 IP，容器重建后 IP 变化但 nginx 不知道。

**解决**：去掉所有 `upstream` 块，改用 Docker 内置 DNS + `set` 变量方式：
```nginx
resolver 127.0.0.11 valid=10s;  # Docker 内置 DNS，每 10 秒重新解析

location / {
    set $upstream_web http://web:80;
    proxy_pass $upstream_web;
}
```

**原理**：`proxy_pass` 用 `set $变量` + `resolver` 指令时，nginx 每次请求都走 Docker DNS 动态解析，容器 IP 变了自动跟新，不再需要重启 nginx。

---

### 问题 14：Langfuse SERVER_IP 变量导致登录重定向失败

**症状**：
- `http://192.168.31.101:3000` 页面能正常打开
- 点击登录后跳转到 `http://106.14.24.38:3000`（ECS IP）然后报错

**根因**：`SERVER_IP=106.14.24.38` 被 Langfuse 的 NextAuth 拼接为 `NEXTAUTH_URL=http://106.14.24.38:3000`，登录成功后 NextAuth 将浏览器重定向到这个地址。但 ECS 上没有穿透 3000 端口，所以重定向失败。

**说明**：`SERVER_IP` 只影响登录回调重定向地址，不影响页面本身能不能访问（Docker 容器监听 `0.0.0.0:3000`，所有 IP 都能访问页面）。

**解决**：在家里内网开发时，将 `.env` 中的 `SERVER_IP` 改为 `192.168.31.101`，这样登录回调跳转到内网 IP。

---

## 修改的文件清单

| 文件 | 改动内容 |
|------|----------|
| `deploy/nginx/nginx.conf` | 去掉 HTTPS 配置，只保留 HTTP 80；改用 `resolver 127.0.0.11` + `set $变量` 动态 DNS |
| `deploy/docker-compose.yml` | nginx 只映射 80 端口，去掉 443 和证书挂载；去掉 Langfuse 的 profiles 配置 |
| `deploy/ecs-nginx/mahongwei.conf` | **新增** ECS nginx SSL 终结配置模板 |
| `docs/deployment.md` | 全面更新：SSL架构改为ECS终结、frpc remotePort=6003、拓扑图、UFW/安全组、验证步骤 |
| `docs/服务地址映射手册.md` | **新增** 各服务访问地址、本地开发 vs 生产部署对照 |
| `docs/docker-镜像源与代理切换.md` | **新增** 三种网络方案切换手册（国内源/代理/裸连） |
| `README.md` | 更新运维命令、加备份还原章节、去掉 --profile langfuse |
| `web/src/pages/HomePage.tsx` | 新增 AutoMind 显眼板块；修复 Link→a 标签导航白屏 |
| `web/src/pages/ai/AIChat.tsx` | 新增每日 5 次对话限制（前端 localStorage + 后端 IP 计数） |
| `ai-server/app/routers/chat.py` | 新增后端每日限制（基于 IP，内网自动绕过）+ chat-count API |
| `vehicle-agent/frontend/src/App.tsx` | Vite base 改为 /vehicle/（修复静态资源 404）；新增每日限制 + 遮罩；更新欢迎消息 |
| `vehicle-agent/frontend/vite.config.ts` | 新增 base: '/vehicle/'（生产环境）解决子路径静态资源 404 |
| `vehicle-agent/backend/app/main.py` | 新增中间件每日限制 + 内网绕过 + chat-count API |
| `deploy/.env.example` | 所有 localhost 改为 192.168.31.101；更新 Langfuse 注释 |

## 当前状态

- [x] frpc 连接 frps 成功（token 已对齐）
- [x] 家里 Docker nginx 正常运行（HTTP-only，无证书错误）
- [x] ECS nginx 监听 80 + 443（SSL 终结）
- [x] frps 监听 6003/6000/6001/6002/7000/7500
- [x] ECS SSL 证书已申请（/etc/letsencrypt/live/mahongwei.com.cn/）
- [x] 全部 14 个 Docker 容器正常运行
- [x] `curl https://mahongwei.com.cn` 完整链路畅通
- [x] nginx 改用 Docker DNS 动态解析，重建容器后无需重启 nginx
- [x] AI 对话 + 车载助手均已加每日 5 次限制（前后端双重保护，内网自动绕过）
- [x] 首页新增 AutoMind 显眼板块（链接改用 a 标签，修复 SPA 导航白屏）
- [x] 车载助手 Vite base 改为 /vehicle/（修复静态资源 404）
### 问题 15：LLM 变量名不一致导致模型 404

**症状**：
- 车机助手发送消息后没反应，Langfuse 无 trace
- vehicle-backend 日志报 `Model not exist` 404 错误

**根因**：
- `deploy/.env` 配置的是 `LLM_DEFAULT_MODEL=deepseek-v4-flash`（ai-server 用的变量名）
- `vehicle-agent/backend/app/config.py` 读取的是 `LLM_MODEL`（不同的变量名）
- `.env` 里没有 `LLM_MODEL`，config.py 用了默认值 `deepseek-v3`
- `deepseek-v3` 在百炼 Token Plan API 上不存在

**解决**：统一变量名，vehicle-agent 代码改用 `LLM_DEFAULT_MODEL`（最小化改动，改代码适配 .env，不改 .env）。涉及文件：config.py、llm.py、supervisor.py、main.py、observability.py、.env、.env.example、test_*.py。

**经验**：多项目共用 .env 时，代码读取的变量名必须和 .env 里的变量名严格一致。不同项目可能用不同的变量名约定。

---

### 问题 16：Langfuse BASE_URL 缺失导致 trace 不发送

**症状**：Docker 部署后车机对话正常但 Langfuse 无 trace。

**根因**：`deploy/.env` 里没有 `LANGFUSE_BASE_URL`，vehicle-backend 容器用了 config.py 默认值 `http://localhost:3000`。在容器内 `localhost` 指的是自己，不是 Langfuse 容器。

**解决**：在 `deploy/.env` 和 `.env.example` 中加 `LANGFUSE_BASE_URL=http://langfuse-app:3000`（Docker 内部容器名通信）。

**经验**：容器间通信必须用 Docker 容器名（如 `langfuse-app`），不能用 `localhost`（指向自身）。

---

### 问题 17：每日限制内存存储重启后丢失

**症状**：被限制 5 次后，容器重建/重启后限制消失。

**根因**：`_vehicle_usage` 和 `_usage` 是 Python 内存 dict，容器重启后内存清空，计数归零。

**解决**：改为文件持久化 `data/usage.json`，每次读写都从文件操作。docker-compose 加了 `ai-data:/app/data` volume 持久化。

**经验**：API 限流计数必须用持久化存储（文件或数据库），内存 dict 容器重启会丢失。

---

### 问题 18：Docker Bridge IP 不在内网白名单导致所有用户被限

**症状**：
- 后端日志 `每日限制达到: ip=172.18.0.12, count=5/5`
- 所有通过 CopilotKit Runtime 的请求都共享同一个 IP

**根因**：
- CopilotKit 请求链路：前端 → Runtime(172.18.0.12) → Backend
- Runtime 不传 X-Forwarded-For，后端只能看到 Runtime 的 Docker bridge IP
- `_is_bypassed` 只检查 192.168/10/127.0.0.1，不检查 172.16-31.x.x

**解决**：
1. `_is_bypassed` 加了 `172.16-31.x.x` Docker bridge IP 白名单
2. nginx copilotkit 路径补了 `X-Forwarded-For` 头
3. `_get_client_ip` 加了 `X-Real-IP` 备选

**说明**：CopilotKit 请求全走 Runtime 代理，后端拿不到真实客户端 IP。加 172.x 绕过 = 所有 /copilotkit 请求不限（合法，因为真正限制靠前端 localStorage 执行）。

---

### 问题 19：本地开发 Langfuse 配置指向外网 + API Key 过期

**症状**：本地开发时 Langfuse 不记录 trace。

**根因**：
1. `vehicle-agent/backend/.env` 的 `LANGFUSE_BASE_URL=http://115.29.236.248:3001`（外网 ECS），改为内网更合理
2. API Key 是旧 V2 注册的，当前 Langfuse 是 V3 实例，旧 Key 无效

**解决**：
1. `LANGFUSE_BASE_URL` 改为 `http://192.168.31.101:3000`（内网直连）
2. API Key 改为 V3 实例的 Key（和 deploy/.env 一致）

---

## 新增修改的文件

| 文件 | 改动内容 |
|------|----------|
| `vehicle-agent/backend/app/config.py` | `LLM_MODEL` → `LLM_DEFAULT_MODEL` |
| `vehicle-agent/backend/app/models/llm.py` | 引用 `settings.LLM_DEFAULT_MODEL` |
| `vehicle-agent/backend/app/graph/supervisor.py` | 引用 `settings.LLM_DEFAULT_MODEL` |
| `vehicle-agent/backend/app/main.py` | 引用 `LLM_DEFAULT_MODEL`；限制改为文件持久化；`_is_bypassed` 加 172.x；`_get_client_ip` 加 X-Real-IP |
| `vehicle-agent/backend/app/utils/observability.py` | 引用 `settings.LLM_DEFAULT_MODEL` |
| `vehicle-agent/backend/.env` | `LLM_MODEL` → `LLM_DEFAULT_MODEL`；`LANGFUSE_BASE_URL` 改内网；Langfuse Key 改 V3 |
| `vehicle-agent/backend/.env.example` | 变量名 + 默认值同步 |
| `vehicle-agent/backend/test_*.py` | 引用 `LLM_DEFAULT_MODEL` |
| `deploy/.env` | 加 `LANGFUSE_BASE_URL=http://langfuse-app:3000`；注释变量名同步 |
| `deploy/.env.example` | 加 `LANGFUSE_BASE_URL`；注释变量名同步 |
| `deploy/nginx/nginx.conf` | copilotkit 路径补 `X-Forwarded-For` |
| `deploy/docker-compose.yml` | ai-server 加 `ai-data:/app/data` volume |
| `ai-server/app/routers/chat.py` | 限制改为文件持久化；`_is_bypassed` 加 172.x；`_get_client_ip` 加 X-Real-IP |
| `vehicle-agent/README.md` | 简化为本地开发 + 部署命令 |

## 当前状态（更新）

- [x] frpc 连接 frps 成功（token 已对齐）
- [x] 家里 Docker nginx 正常运行（HTTP-only，无证书错误）
- [x] ECS nginx 监听 80 + 443（SSL 终结）
- [x] frps 监听 6003/6000/6001/6002/7000/7500
- [x] ECS SSL 证书已申请（/etc/letsencrypt/live/mahongwei.com.cn/）
- [x] 全部 Docker 容器正常运行
- [x] `curl https://mahongwei.com.cn` 完整链路畅通
- [x] nginx 改用 upstream 块（去掉 resolver），重建容器后需手动 restart nginx
- [x] AI 对话 + 车载助手每日限制改为文件持久化（不丢失）
- [x] 内网 IP + Docker bridge IP(172.x) 自动绕过限制
- [x] 首页 AutoMind 板块（a 标签导航）
- [x] 车机助手 Vite base /vehicle/（静态资源正常）
- [x] LLM 变量名统一为 `LLM_DEFAULT_MODEL`
- [x] Langfuse BASE_URL 配置（Docker 用容器名，本地用内网 IP）
- [x] Langfuse API Key 统一为 V3 实例的 Key
- [ ] 部署后验证：vehicle-backend force-recreate 后限制和 Langfuse 是否正常
