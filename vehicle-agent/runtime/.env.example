# =============================================================
# 本文件仅用于本地开发参考
# 
# 生产部署请使用统一的配置文件：
#   deploy/.env.example  → 复制为 deploy/.env
#
# 本项目的配置项已包含在 deploy/.env.example 中：
#   - RUNTIME_PORT
#   - PYTHON_AGENT_URL (由 docker-compose 自动注入)
# =============================================================

# 本地开发时，复制此文件为 .env 并填入真实值

RUNTIME_PORT=4000
PYTHON_AGENT_URL=http://localhost:8001/copilotkit
